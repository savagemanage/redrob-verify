# Local only — copy to `.gpu.env` (gitignored). Never commit real host/key paths.

GPU_SSH_HOST=
GPU_SSH_USER=ubuntu
GPU_SSH_KEY=
GPU_REPO_DIR=redrob-verify

# Forgery TruFor (GPU host compose). Path to TruFor/test_docker/src on the remote.
# TRUFOR_SRC=/home/ubuntu/TruFor/test_docker/src
# FORGERY_BACKEND=trufor
